// App-wide configuration constants.

// Download URL for the local background-removal module (rembg) executable.
// After you publish the built .exe as a GitHub Release asset, point this at it.
// Easiest: keep a "latest" release and link to its page.
export const LOCAL_MODULE_DOWNLOAD_URL =
  'https://github.com/izanled/GameImageEditer/releases/latest'
